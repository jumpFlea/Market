<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qst.dao.UserGoodsDAO">

    <insert id="saveUserGoods" parameterType="Goods">
        insert into
        user_goods(g_id,u_id,ug_date)
        VALUES(#{goodsId},#{userId},#{date})
    </insert>
</mapper>